# Python agent package definition.
#
# Will define:
#   - packages.agent     — the PydanticAI orchestrator agent, packaged as a Nix derivation
#   - devShells.agent    — Python-specific dev shell (if distinct from default)
#
# Deferred until agent implementation begins.
{ ... }:
{
}
