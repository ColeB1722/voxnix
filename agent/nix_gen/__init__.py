"""nix_gen — JSON spec generation for voxnix mkContainer.

This package owns the Python side of the agent-to-Nix boundary:
- Pydantic models defining container specs
- Module discovery (queries flake outputs)
- JSON serialization for Nix consumption
"""
